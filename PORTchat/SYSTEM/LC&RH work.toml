[tool.poetry]
name = "PORTchat"
version = "1.0.0"
description = "An easy-to-use Python chat!"
authors = ["LiteCode (Tryhub Evgeniy) and Harchenko Rostislav"]

[tool.poetry.dependencies]
python = "^3.8"
replit = "^2.0.0"
ansicolors = "^1.1.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
